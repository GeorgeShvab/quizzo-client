import { FC } from 'react'
import Loader from './Loader'

const LoaderDisplay: FC = () => {
  return (
    <div className="loader-container">
      <Loader />
    </div>
  )
}

export default LoaderDisplay
